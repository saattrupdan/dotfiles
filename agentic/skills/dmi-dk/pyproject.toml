[project]
name = "dmi-dk"
version = "1.0.0"
description = "CLI for DMI (Danmarks Meteorologiske Institut) weather data"
requires-python = ">=3.12"

[project.scripts]
dmi = "dmi_dk.main:main"

[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"
