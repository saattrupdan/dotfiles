---
description: Call the planner to produce a plan for the given request.
---

Call the `subagent` tool with `agent: "planner"`, a 1–5-word `taskName` summarising
`$@`, and `task: "$@"`. Report the plan back to the user.
